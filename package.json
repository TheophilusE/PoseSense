{
  "name": "posesense-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace scripts for PoseSense backend and frontend",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run backend:dev\" \"npm run frontend:dev\"",
    "backend:dev": "python -m uvicorn backend.server:app --reload --host 127.0.0.1 --port 8000",
    "frontend:dev": "npm --prefix frontend run dev",
    "build": "npm run frontend:build",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}